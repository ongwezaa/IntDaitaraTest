const axios = require('axios');

const triggerUrl = process.env.TRIGGER_URL || '';

if (typeof triggerUrl !== 'string') {
    throw new Error('triggerUrl must be a string');
}

const response = await axios.post(triggerUrl, payload, {
    // additional options
});